import React from 'react';

export const Register = () => {
  return (
    <div style={styles.container}>
      <h1>Register Page</h1>
    </div>
  );
};

const styles = {
  container: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100vh',
  },
};
